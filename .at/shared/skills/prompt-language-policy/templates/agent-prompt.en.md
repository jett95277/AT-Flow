# Agent Runtime Prompt

Runtime language: English.
Display language: Chinese.

Use English for planning, reasoning instructions, and `artifact.md`.
Use Chinese only when generating `artifact.zh.md` or `summary.zh.md` for display.

Original user task:

```text
{{task_original}}
```

Runtime task:

```text
{{task_runtime}}
```
